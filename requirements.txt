# Archivo requirements.txt
# Dependencias compatibles con Streamlit Cloud y Python 3.12
streamlit>=1.25,<2.0
# numpy 1.25.x distribuye ruedas para Python 3.12
numpy>=1.25,<1.26
pillow>=10.0,<11.0
# Usar tensorflow-cpu para evitar dependencias de GPU en Cloud
tensorflow-cpu>=2.13,<2.14
pandas>=2.0,<2.1
openpyxl>=3.1,<3.2
# Necesarios para cargar modelos Keras (.keras / HDF5)
h5py>=3.8,<4.0
# Para reintroducir distutils en Python 3.12
egg-info-mgr>=0.1  # alternativa ligera a setuptools/distutils

# Para instalar:
# pip install -r requirements.txt
